import { HavenApp } from '@/components/app/HavenApp'

/**
 * Catch-all page for the main app routes.
 * All routes under (main) are handled by HavenApp's client-side routing.
 */
export default function MainAppPage() {
  return <HavenApp />
}
